    //Nomes: Isadellis Paixão, Milena Bueno
    #include <bits/stdc++.h>
    using namespace std;

    #define _ ios_base::sync_with_stdio(0); cin.tie(0);
    #define endl "\n"
    #define ll long long

    int main (void) {_
        ll a, b, c;

        while ((cin >> a >> b) && (a || b)){
            c = (2LL * a - b);

            cout << c << endl;
        }
        

        return 0;
    }